/target
/node_modules
/dist
*.log
.DS_Store
src-tauri/target/
package-lock.json
.winget
certs/
.wibey/
.claude/
videos/

# Tauri regenerates these on every build; they churned in every worktree and PR.
src-tauri/gen/schemas/

# Where the iPhone CI job puts what it built and photographed
# (.github/workflows/ios.yml). Uploaded as run artifacts, never committed.
/artifacts/
/build/

# The Xcode project's link to the built frontend. A symlink to `dist/`,
# remade by scripts/ci/ios-ensure-assets.sh on every build — see the comment
# in that file for why it is not committed.
src-tauri/gen/apple/assets

# Seeded design-canvas output — regenerated from the .dc.html working files
design/**/yames-app-redesign.html
design/**/yames-site-directions.html

# Local-only browser preview shim (see src/devShim.local.ts)
src/devShim.local.ts

# Python bytecode from the sound generators in scripts/
__pycache__/
*.pyc

# Playwright: the browser it downloads, and what a failing layout run leaves
# behind (screenshots and traces of the moment it failed).
test-results/
playwright-report/
blob-report/
playwright/.cache/
