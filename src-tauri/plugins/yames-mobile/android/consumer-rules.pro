# Kept because the Android Gradle plugin's `consumerProguardFiles` wants the
# file to exist. The plugin class and its commands are reached by reflection
# from Tauri's PluginManager; the app's `proguard-tauri.pro` covers that.
