# Nothing to keep beyond what the Tauri plugin manager already reflects on:
# `proguard-tauri.pro` (applied to the app) keeps @TauriPlugin classes and
# their @Command methods. The service is named in the manifest, which R8 reads.
